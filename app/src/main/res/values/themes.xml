<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Post-splash app theme (Compose content). -->
    <style name="Theme.Navi" parent="@android:style/Theme.Material.Light.NoActionBar" />

    <!-- Cold-start splash (Androidx Splash Screen API; API 26+ via compat). -->
    <style name="Theme.Navi.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Navi</item>
    </style>
</resources>
